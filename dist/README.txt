Copy contents of this whole folder into any desireable place on your disk (which should be writeable), change directory to that new location and execute install.bat and then start.bat. This will install, activate and start currency download service.
